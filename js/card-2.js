document.getElementById('btn-apply').addEventListener('click', function(){
    const couponField = document.getElementById('text-coupon');
    const couponCode = couponField.Value;
    const discountField = document.getElementById('discount-field')
    const discountAmountString = discountField.innerText;
    const discountAmount = parseFloat.discountAmountString;
    const afterDiscountPriceString = (regularPrice / 100) * 70;
    if(couponCode === 'DISC30'.toUpperCase){
        
    }
})







document.getElementById('text-coupon'),addEventListener('keyup', function(event){
    const text = 
})